info:
  name: Unknown Customer 404
  type: http
  seq: 4

http:
  method: GET
  url: "{{baseUrl}}/v1/khata/00000000-0000-0000-0000-000000000000"
  auth:
    type: bearer
    token: "{{authToken}}"

runtime:
  scripts:
    - type: after-response
      code: |-
        // Negative test: unknown customer id. Expect 404.
        console.log(res.getStatus(), JSON.stringify(res.getBody()));

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
