info:
  name: Khata No Auth 401
  type: http
  seq: 5

http:
  method: GET
  url: "{{baseUrl}}/v1/khata/{{customerId}}"
  auth:
    type: none

runtime:
  scripts:
    - type: after-response
      code: |-
        // Negative test: no Authorization header. Expect 401.
        console.log(res.getStatus(), JSON.stringify(res.getBody()));

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
