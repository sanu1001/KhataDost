info:
  name: List of Items
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseUrl}}/v1/inventory"
  auth:
    type: bearer
    token: "{{authToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
