info:
  name: Scan No Auth 401
  type: http
  seq: 2

http:
  method: POST
  url: "{{baseUrl}}/v1/scan"
  body:
    type: json
    data: |-
      {
        "image_base64": "aGVsbG8="
      }
  auth:
    type: none

runtime:
  scripts:
    - type: after-response
      code: |-
        // Negative test: no Authorization header. Expect 401.
        console.log(res.getStatus(), JSON.stringify(res.getBody()));

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
