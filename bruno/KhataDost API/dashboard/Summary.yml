info:
  name: Summary
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}/v1/dashboard/summary"
  auth:
    type: bearer
    token: "{{authToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
