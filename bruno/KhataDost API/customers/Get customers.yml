info:
  name: Get customers
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseUrl}}/v1/customers"
  auth:
    type: bearer
    token: "{{authToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
